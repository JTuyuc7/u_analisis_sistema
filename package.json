{
  "name": "proyecto_final",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "backend/**/*.{ts,tsx}": [
      "cd backend && npm run lint -- --max-warnings=0"
    ]
  }
}
